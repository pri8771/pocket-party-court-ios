import Foundation
import Observation

@Observable
final class HomeViewModel {
    var headline = "Court is now in session."
    var subtitle = "Put silly disputes on trial, vote locally, and crown a winner."
    var playerNames = ["Alex", "Sam"]
}
